# Development points at the deployed worker too, so no machine here needs the
# IGDB credential. Delete this line (or set it empty) to fall back to the local
# proxy in vite.config.js, which is the only reason to keep a credential on disk.
VITE_PROXY_ORIGIN=https://lorehaven-proxy.nishilvani.workers.dev
